/** DOM:
 * permite representar el documento HTML y acceder a sus elementos para manipularlos. Manejamos propiedades metodos y eventos
 * Representa los elem como nodos y objetos .
 * Nodo es un punto especifico del dom: documento, elementos html, texto comentarios. 
 * 
 * Root node: HTML. noto raiz, contienea todos los elementos. 
 * Parent node (nodo padre): contiene a otro nodo en la jerarquia del DOM. 
 * Child node: contenido dentro de un nodo. 
 * Descendent node : nodo contnido dentro de otro nodo (directa o indirectam) 
 * sibling nodes: nodos hermanos, nodos ubicados en el mismo nivel de jerarquia 
 * 
 */


